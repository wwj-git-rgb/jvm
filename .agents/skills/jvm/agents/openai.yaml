interface:
  display_name: "Jvm"
  short_description: "Repo-specific patterns and workflows for jvm"
  default_prompt: "Use the jvm repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true